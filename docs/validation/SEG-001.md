# Validación SEG-001

## Estado

`PENDING_EXECUTION — STATIC_AUTOMATION_PARTIAL_PASS — CONSOLIDATED_IN_MAIN`

Este archivo distingue evidencia ejecutada, revisión estática y expectativas. Una prueba versionada no equivale a una prueba aprobada.

## Rama canónica

```text
main
```

## Consolidación verificada

- fecha: `2026-07-20`;
- `main` anterior: `7fca32b9b777f8b8c5c305c0699d14877babebc6`;
- árbol consolidado: `1c089cd19ce953e93531c1ef3c54f79d28575444`;
- avance fast-forward con `force=false`;
- comparación previa: 171 commits adelante y 0 atrás;
- comparación posterior inmediata: ramas idénticas;
- evidencia: `docs/main-consolidation.md`.

Los cambios posteriores se realizaron directamente en `main`.

## Último commit con estado de GitHub consultado

```text
f2cddf2c9ca37416470d167e16b4a552d2839037
```

Resultado observado:

```text
combined statuses: []
workflow runs visibles: []
```

Toda ejecución futura debe registrar su propio `git rev-parse HEAD`.

## Evidencia estática ejecutada

Documento detallado:

```text
docs/validation/SEG-001-static-automation-2026-07-20.md
```

Resultados:

```text
docker-compose.yml: YAML parse PASS
ci.yml: YAML parse PASS
servicios Compose esperados: PASS
jobs CI esperados: PASS
preflight.sh sh -n: PASS
smoke-test.sh sh -n: PASS
Makefile make -n: PASS
```

Estos resultados comprueban sintaxis y estructura. No sustituyen Docker Compose, builds, PowerShell, Maven, npm o smoke test real.

## Matriz de controles

| Control | Comando/job | Estado | Evidencia |
|---|---|---|---|
| Consolidación en `main` | compare + update ref | PASS | Fast-forward y comparación idéntica |
| Configuración DB local | `.env.example`/Compose/backend | PASS_STATIC | Variables coherentes |
| Documentación de arranque | guías y README | PASS_STATIC | Dos modalidades y troubleshooting |
| Contexto Docker backend | `.dockerignore` | PASS_STATIC | Secretos, datos y cachés excluidos |
| Contexto Docker frontend | `frontend/.dockerignore` | PASS_STATIC | Secretos, datos y cachés excluidos |
| Finales de línea | `.gitattributes` | PASS_STATIC | Reglas Unix/Windows versionadas |
| Compose YAML | PyYAML | PASS_SYNTAX | Servicios esperados presentes |
| Workflow YAML | PyYAML | PASS_SYNTAX | Jobs esperados presentes |
| Scripts Unix | `sh -n` | PASS_SYNTAX | Ambos scripts parsean |
| Makefile | `make -n` | PASS_SYNTAX | Targets representativos parsean |
| Scripts PowerShell | parser PowerShell | IMPLEMENTED_NOT_RUN | `pwsh` ausente localmente |
| Maven/compilación | `mvnw ... verify` | BLOCKED | Sin Maven/caché/red en entorno actual |
| Spotless | fase `verify` | BLOCKED | Depende de Maven |
| Unit tests | Surefire | IMPLEMENTED_NOT_RUN | Regresiones versionadas |
| ArchUnit | Surefire | IMPLEMENTED_NOT_RUN | Regla versionada |
| Testcontainers | Surefire + Docker | BLOCKED | Docker ausente |
| Flyway V1–V5 | Testcontainers | IMPLEMENTED_NOT_RUN | Migraciones revisadas estáticamente |
| Hibernate validate | Testcontainers | IMPLEMENTED_NOT_RUN | Entidades cruzadas con esquema |
| Frontend install | `npm install` | BLOCKED | Registro/caché no disponibles |
| Frontend lockfile | `package-lock.json` | MISSING | Debe generarse y versionarse |
| Frontend typecheck | `npm run typecheck` | BLOCKED | Dependencias no instaladas |
| Frontend build | `npm run build` | BLOCKED | Dependencias no instaladas |
| Preflight local | scripts `--local` | NOT_RUN | Requiere herramientas y Docker |
| Preflight container-only | scripts | NOT_RUN | Requiere Docker |
| Compose semántico | `docker compose --profile app config` | BLOCKED | Docker/Compose ausentes |
| Imagen backend | `docker build` | BLOCKED | Docker ausente |
| Imagen frontend | Dockerfile frontend | BLOCKED | Docker/red ausentes |
| Stack PostgreSQL/backend/frontend | perfil `app` | NOT_RUN | Requiere imágenes y Docker |
| Health checks | Compose/containers | NOT_RUN | Requiere stack activo |
| Smoke test Unix | `scripts/smoke-test.sh` | NOT_RUN | Requiere stack activo |
| Smoke test Windows | `scripts/smoke-test.ps1` | NOT_RUN | Requiere stack activo |
| GitHub Actions | workflow `ci` | NO_VISIBLE_RUN | Sin estados o runs visibles |
| Datos sensibles | búsqueda remota | PASS_STATIC | Sin datos reales versionados |
| Envío real ausente | inspección del árbol | PASS_BY_DESIGN | Sin adaptador de correo |

## CI implementado

El workflow contiene:

1. backend con Maven verify;
2. frontend con install, typecheck y build;
3. sintaxis de scripts Unix;
4. sintaxis de scripts PowerShell;
5. preflight fail-closed con credenciales ficticias;
6. validación de Compose con perfil `app`;
7. build de imagen backend;
8. build de imagen frontend.

El caché npm se mantiene desactivado hasta disponer de `package-lock.json`.

## Hallazgos estáticos corregidos

1. exclusiones importadas deshabilitan prospectos existentes y generan auditoría;
2. preview ambiguo persiste `DuplicateReview`;
3. duplicado exacto conserva referencia al prospecto;
4. multipart y servicio comparten límite de 10 MB y HTTP 413;
5. filas se ordenan por hoja y número;
6. correo se valida antes de persistencia;
7. evidencia de rechazo usa normalización tolerante;
8. CSV admite coma y punto y coma y rechaza estructuras inválidas;
9. fechas Excel se convierten en UTC;
10. preview aplica exclusiones sin escritura de dominio;
11. filas bloqueadas utilizan `excludedRows` separado;
12. nombre de archivo se sanea;
13. Basic Auth frontend usa UTF-8;
14. configuración PostgreSQL backend/Compose quedó alineada;
15. `main` quedó como única fuente canónica;
16. stack completo quedó disponible bajo perfil Compose `app`;
17. backend y frontend incorporaron health checks;
18. sintaxis PowerShell del smoke test fue corregida;
19. CI evita caché npm sin lockfile;
20. contextos Docker excluyen secretos y datos.

## Pruebas y controles versionados

- exclusión importada retroactiva y auditada;
- preview con revisión ambigua;
- duplicado exacto enlazado;
- correo malformado aislado por fila;
- preview con fila bloqueada sin escritura de dominio;
- conteo de bloqueados independiente;
- CSV con punto y coma y campos citados;
- CSV mal formado;
- encabezados normalizados duplicados;
- normalización inválida;
- persistencia, exclusiones, seguridad y arquitectura;
- preflight fail-closed;
- smoke test de health, API autenticada y frontend.

## Evidencia estructural

- código y documentación vigentes en `main`;
- Flyway V1–V5 aditivo;
- configuración de envío cerrada;
- fixtures con dominios de prueba;
- lote real ausente del árbol;
- Maven Wrapper con hash fijado;
- perfil Compose con tres servicios;
- puertos ligados a loopback;
- health checks encadenados;
- imágenes backend/frontend multi-stage;
- proxy Nginx hacia backend;
- scripts Unix/PowerShell y Makefile;
- workflow de cuatro áreas de validación;
- sin acción de envío real.

## Bloqueo externo reproducido

```text
Java: presente
Node/npm: presentes
sh/make/PyYAML: presentes
PowerShell: ausente
Maven: ausente
Docker/Compose: ausentes
caché Maven: vacía
caché npm: vacía
acceso a registros: no disponible en el contenedor
workflow dispatch mediante conector: no disponible
estados visibles: ninguno
```

## Protocolo para convertir a PASS

1. clonar `main` en un entorno con red y Docker;
2. registrar SHA exacto;
3. configurar `.env` manteniendo envíos cerrados;
4. ejecutar preflight;
5. ejecutar Maven, formato, tests y Testcontainers;
6. confirmar Flyway y Hibernate;
7. instalar frontend y generar lockfile;
8. ejecutar typecheck y build;
9. cambiar a `npm ci` después de versionar lockfile;
10. validar Compose completo;
11. construir ambas imágenes;
12. levantar el stack;
13. ejecutar smoke test;
14. revisar datos sensibles;
15. corregir fallos con regresión;
16. completar esta tabla con fecha, SHA, run y salida;
17. repetir toda la matriz;
18. marcar SEG-001 completo solo con controles principales en `PASS`.

## Condición de cierre

No se cumple todavía. `PASS_BY_DESIGN` solo se acepta para la ausencia deliberada de envío real.
