# Validación SEG-001

## Estado

```text
PENDING_COMPLETE_LOCAL_VALIDATION
AUTOMATION_IMPLEMENTED_WINDOWS_AND_UNIX
LATEST_REAL_RUN=FAIL_POWERSHELL_PARSE
PARSER_FIX_COMMITTED
CLEAN_BUILDS_PENDING
LOCKFILE_PENDING
CI_NOT_VISIBLE
CONSOLIDATED_IN_MAIN
```

Una automatización implementada no equivale a una ejecución verde. Una imagen exportada desde caché no equivale a un build limpio.

## Rama

```text
main
```

La rama histórica está detrás y no contiene cambios exclusivos.

## Evidencias versionadas

```text
docs/validation/SEG-001-static-automation-2026-07-20.md
docs/validation/SEG-001-container-build-2026-07-20.md
docs/validation/SEG-001-rerun-2026-07-20.md
docs/validation/SEG-001-local-orchestration-2026-07-20.md
docs/validation/SEG-001-complete-validation-automation-2026-07-20.md
docs/validation/SEG-001-cross-platform-validation-2026-07-20.md
docs/validation/SEG-001-powershell-parser-failure-2026-07-21.md
```

## Ejecución real acumulada

### Intento 1 — frontend

```text
preflight: PASS
guardas: PASS
npm install: PASS
frontend TypeScript/build: FAIL
backend: CANCELED
stack: NOT_RUN
smoke: NOT_RUN
```

Errores corregidos:

- credenciales anulables en callbacks;
- import CSS sin declaración.

### Intento 2 — Compose

```text
frontend image: PASS_FROM_CACHE
backend image: PASS_FROM_CACHE
clean builds: NO DEMOSTRADOS
stack: FAIL por puerto host 5432
servicios: NOT_STARTED
Flyway/Hibernate/smoke: NOT_RUN
```

Hardening posterior:

- tres puertos configurables;
- URL DB coordinada;
- puertos distintos;
- publicación en loopback;
- preflight de daemon y Compose.

### Intento 3 — PowerShell, 2026-07-21

El checkout se actualizó correctamente mediante fast-forward.

El validador integral se lanzó dos veces y falló durante el parseo:

```text
scripts/validate-seg001.ps1:21
InvalidVariableReferenceWithDrive
$LASTEXITCODE:
```

Estado:

```text
EXECUTED_FAIL — POWERSHELL_PARSE_ERROR
```

Alcance no alcanzado:

```text
preflight: NOT_RUN
Compose config: NOT_RUN
builds: NOT_RUN
stack: NOT_RUN
health: NOT_RUN
smoke: NOT_RUN
Maven/Testcontainers: NOT_RUN
lockfile/npm ci: NOT_RUN
```

No hubo transcript o JSON porque el parser falló antes de ejecutar el script.

Correcciones aplicadas:

```text
scripts/validate-seg001.ps1
scripts/validate-docker-stack.ps1
scripts/verify-backend-container.ps1
scripts/check-powershell-syntax.ps1
.github/workflows/ci.yml
mvnw.cmd
```

El mensaje ambiguo fue reemplazado por `-f` y CI rechaza `$LASTEXITCODE:`.

## Matriz actual

| Control | Estado | Evidencia/acción |
|---|---|---|
| Consolidación en main | PASS | fast-forward y comparación |
| Rama histórica sin trabajo exclusivo | PASS | comparación de refs |
| Guardas de envío | PASS | preflight real inicial |
| Sin adaptador de envío | PASS_BY_DESIGN | arquitectura |
| Datos reales fuera de Git | PASS_STATIC | reglas y revisión |
| Correcciones TypeScript | PASS_CODE_REVIEW | commits/read-back |
| Compose cuatro servicios | PASS_STRUCTURE | YAML |
| Tres puertos configurables | PASS_CODE_REVIEW | env/Compose/scripts |
| Configurador Unix | PASS_FUNCTIONAL_ISOLATED | `.env` temporal |
| Configurador PowerShell | PENDING_RERUN | siguiente preflight |
| Preflight inicial | PASS | ejecución aportada |
| Preflight endurecido | NOT_RUN | parser bloqueó antes |
| Parser PowerShell intento 3 | EXECUTED_FAIL | evidencia 2026-07-21 |
| Arreglo `$LASTEXITCODE:` | PASS_CODE_REVIEW | tres scripts corregidos |
| Checker PowerShell local | IMPLEMENTED_NOT_RUN | ejecutar tras pull |
| Regresión CI de interpolación | PASS_CODE_REVIEW | grep exacto |
| Normalización `mvnw.cmd` | PASS_CODE_REVIEW | blob reescrito |
| Frontend image cacheada | PASS_FROM_CACHE | segunda ejecución |
| Backend image cacheada | PASS_FROM_CACHE | segunda ejecución |
| Frontend clean build | NOT_RUN | validador pendiente |
| Backend clean build | NOT_RUN | validador pendiente |
| PostgreSQL health | NOT_RUN | validador pendiente |
| Backend health | NOT_RUN | validador pendiente |
| Frontend health | NOT_RUN | validador pendiente |
| Flyway arranque | NOT_RUN | stack pendiente |
| Hibernate validate | NOT_RUN | stack pendiente |
| Smoke host | NOT_RUN | stack pendiente |
| Smoke contenedor | NOT_RUN | stack pendiente |
| Maven verify | NOT_RUN | parser bloqueó antes |
| Spotless | NOT_RUN | Maven verify |
| Unit tests | NOT_RUN | Maven verify |
| ArchUnit | NOT_RUN | Maven verify |
| Testcontainers | NOT_RUN | Maven verify |
| package-lock real | MISSING | validador pendiente |
| npm ci real | NOT_RUN | después de lockfile |
| Seguridad del repositorio | NOT_RUN_REAL | fase final |
| Evidencia JSON/transcript integral | NOT_GENERATED | parser falló antes |
| CI visible | NO_VISIBLE_RUN | conector sin estados |

## Cambios correctivos acumulados

1. exclusiones retroactivas y auditadas;
2. revisiones ambiguas en preview;
3. duplicados exactos enlazados;
4. límite 10 MB y HTTP 413;
5. orden hoja/fila;
6. recuperación por fila;
7. parser CSV endurecido;
8. fechas Excel UTC;
9. preview con exclusiones;
10. métrica `Bloqueadas`;
11. filenames seguros;
12. Basic Auth UTF-8;
13. DB local coherente;
14. `main` canónico;
15. perfiles app/smoke;
16. imágenes y health checks;
17. smoke host/contenedor;
18. contextos Docker seguros;
19. nullability frontend;
20. tipos Vite/CSS;
21. lockfile mediante Docker;
22. puertos host configurables;
23. preflight fail-fast;
24. evidencia JSON;
25. Maven/Testcontainers contenedorizado;
26. lockfile sin scripts lifecycle;
27. UID/GID preservado en Unix;
28. validadores integrales Windows/Unix;
29. seguridad centralizada;
30. corrección de interpolación PowerShell;
31. checker local de sintaxis;
32. regresión CI;
33. normalización de `mvnw.cmd`.

## Ejecución canónica siguiente

```powershell
Set-Location C:\laburo\crm-platform

git switch main
git fetch origin
git pull --ff-only

git restore --source=origin/main --staged --worktree -- mvnw.cmd
git update-index --refresh
git status --short

powershell -ExecutionPolicy Bypass -File scripts/check-powershell-syntax.ps1
powershell -ExecutionPolicy Bypass -File scripts/preflight.ps1 -ContainerOnly

powershell -ExecutionPolicy Bypass -File scripts/validate-seg001.ps1 `
  -PostgresPort 55432 `
  -BackendPort 8080 `
  -FrontendPort 5173 `
  -KeepRunning
```

## Evidencia esperada

```text
validation-output/seg001-docker-*.json
validation-output/seg001-complete-*.json
validation-output/seg001-complete-*.log
frontend/package-lock.json
```

## Protocolo después de PASS

1. revisar JSON;
2. revisar transcript;
3. revisar lockfile;
4. versionar únicamente lockfile;
5. repetir desde árbol limpio;
6. confirmar npm ci desde el primer build;
7. observar CI;
8. actualizar esta matriz;
9. cerrar SEG-001.

## Condición de cierre

- [ ] checker PowerShell PASS;
- [ ] preflight actualizado PASS;
- [ ] clean frontend PASS;
- [ ] clean backend PASS;
- [ ] tres servicios healthy;
- [ ] Flyway PASS;
- [ ] Hibernate validate PASS;
- [ ] smoke host PASS;
- [ ] smoke container PASS;
- [ ] Maven verify PASS;
- [ ] Spotless PASS;
- [ ] unit tests PASS;
- [ ] ArchUnit PASS;
- [ ] Testcontainers PASS;
- [ ] package-lock versionado;
- [ ] npm ci PASS con lockfile versionado;
- [ ] seguridad PASS;
- [ ] evidencia registrada;
- [ ] CI visible verde o excepción documentada;
- [ ] SEG-001 COMPLETE;
- [ ] SEG-002 ACTIVE.

## Seguridad

```text
SENDING_ENABLED=false
SENDING_DRY_RUN=true
SENDING_DAILY_LIMIT=0
SENDING_KILL_SWITCH=true
```

El intento de parser no ejecutó Docker, no modificó datos y no produjo comunicaciones.
