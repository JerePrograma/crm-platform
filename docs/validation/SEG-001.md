# Validación SEG-001

## Estado

`PENDING_EXECUTION`

Este archivo registra evidencia ejecutada. No convertir una expectativa en resultado.

## Rama

```text
feat/seg-001-prospect-vertical-slice
```

## Último commit revisado en esta iteración

```text
4a616607952b7b81f9c02fe632c920f9738ddd33
```

## Controles

| Control | Comando/job | Estado | Evidencia |
|---|---|---|---|
| Maven/compilación | `sh ./mvnw -B -f backend/pom.xml verify` | PENDING | Sin ejecución visible |
| Spotless | fase `verify` | PENDING | Sin ejecución visible |
| Unit tests | Maven Surefire | PENDING | Pruebas versionadas |
| Testcontainers | Maven Surefire + Docker | PENDING | Pruebas versionadas |
| Flyway | contextos Testcontainers | PENDING | V1–V4 versionadas |
| Hibernate validate | contextos Testcontainers | PENDING | Configurado |
| Frontend TypeScript | `npm run build` | PENDING | Código versionado |
| Compose | `docker compose config` | PENDING | Archivo versionado |
| Imagen backend | `docker build ...` | PENDING | Dockerfile versionado |
| Secret/data scan | revisión/búsqueda | PARTIAL | XLSX real no versionado |
| Envío real ausente | inspección de árbol | PASS_BY_DESIGN | Sin Gmail/SMTP/mail adapter |

## Evidencia estructural disponible

- rama por delante de `main` y sin divergencia;
- archivos remotos leídos después de escritura;
- configuración de envío cerrada;
- migraciones V1–V4 presentes;
- fixtures con dominios `.test`;
- XLSX real excluido;
- CI configurado para backend, frontend, Compose e imagen;
- Maven launcher con SHA-512 fijado.

## Resultados de CI

Todavía no se observan checks asociados al último commit mediante las herramientas disponibles.

## Protocolo ante fallo

1. registrar run/job/commit;
2. copiar solo el fragmento relevante del error, sin secretos;
3. identificar causa raíz;
4. corregir en la misma rama;
5. añadir prueba de regresión cuando aplique;
6. actualizar esta tabla;
7. no mover SEG-002 a activo hasta verde.

## Condición de PASS

Todos los controles técnicos principales deben estar `PASS` con fecha, commit y evidencia. `PASS_BY_DESIGN` solo se acepta para ausencia de envío porque no existe el componente.
