# Validación SEG-001

## Estado

`PENDING_AUTOMATED_DOCKER_RERUN — LOCAL_PORTS_HARDENED — NPM_CI_READY — CONSOLIDATED_IN_MAIN`

Una imagen exportada desde caché no equivale a una compilación limpia. Un script implementado y revisado tampoco equivale a una ejecución verde.

## Rama

```text
main
```

## Evidencias

```text
docs/validation/SEG-001-static-automation-2026-07-20.md
docs/validation/SEG-001-container-build-2026-07-20.md
docs/validation/SEG-001-rerun-2026-07-20.md
docs/validation/SEG-001-local-orchestration-2026-07-20.md
```

## Ejecución real acumulada

### Entorno aportado

```text
Windows PowerShell
Docker 29.3.1 build c2be9cc
modo container-only
```

### Primer intento

```text
preflight: PASS
guardas: PASS
npm install: PASS
frontend TypeScript/build: FAIL
backend: CANCELED
stack: NOT_RUN
smoke: NOT_RUN
```

Errores frontend:

```text
Credentials | null en getProspect
Credentials | null en refresh
import CSS sin declaración Vite/TypeScript
```

Correcciones:

```text
72f0421caaf4898ce04fd97724ecbad9a4ed6390
31960db073d6df0cae683b02267a752b9538e08f
```

### Segunda reejecución

Frontend:

```text
imagen exportada: PASS_FROM_CACHE
npm run build: CACHED
clean build: NO DEMOSTRADO
```

Backend:

```text
imagen exportada: PASS_FROM_CACHE
mvn package: CACHED
clean build: NO DEMOSTRADO
pruebas: NOT_RUN
```

Arranque:

```text
docker compose --profile app up -d: FAIL
causa: 127.0.0.1:5432 no disponible
postgres/backend/frontend: NOT_STARTED
Flyway/Hibernate/smoke: NOT_RUN
```

## Hardening posterior implementado

### Puertos

```text
POSTGRES_HOST_PORT
BACKEND_HOST_PORT
FRONTEND_HOST_PORT
```

- rangos 1–65535;
- valores distintos;
- publicación solo en loopback;
- `DATABASE_URL` coordinada con PostgreSQL;
- red interna sin cambios.

### Scripts

```text
scripts/set-local-host-ports.ps1
scripts/set-local-host-ports.sh
scripts/validate-docker-stack.ps1
```

- actualización segura de `.env`;
- preservación de credenciales;
- preservación de `SENDING_*`;
- UTF-8 sin BOM en PowerShell;
- clean builds por defecto;
- espera de health;
- smoke host y contenedor;
- transcript local ignorado por Git;
- cleanup sin borrar volumen.

### Compatibilidad

```text
scripts/set-postgres-host-port.ps1
scripts/set-postgres-host-port.sh
```

continúan funcionando y delegan al configurador coordinado.

### npm

Dockerfile, CI y Makefile seleccionan:

```text
package-lock presente -> npm ci
package-lock ausente  -> npm install
```

La generación del lockfile sigue pendiente.

## Matriz actual

| Control | Estado | Evidencia |
|---|---|---|
| Consolidación en main | PASS | fast-forward y comparación |
| Guardas de envío | PASS | preflight real |
| Preflight PowerShell inicial | PASS | salida aportada |
| Correcciones TypeScript | PASS_CODE_REVIEW | commits y read-back |
| `excludedRows` visible | PASS_CODE_REVIEW | UI `Bloqueadas` |
| Puerto PostgreSQL configurable | PASS_CODE_REVIEW | Compose/env/scripts |
| Puerto backend configurable | PASS_CODE_REVIEW | Compose/env/preflight |
| Puerto frontend configurable | PASS_CODE_REVIEW | Compose/env/preflight |
| Puertos distintos y validados | PASS_CODE_REVIEW | preflight Unix/PowerShell |
| Smoke URLs variables | PASS_CODE_REVIEW | scripts host |
| Configurador Unix tres puertos | PASS_FUNCTIONAL_ISOLATED | `.env` temporal |
| Preservación de secretos ficticios | PASS_FUNCTIONAL_ISOLATED | prueba temporal |
| Configurador PowerShell tres puertos | PENDING_REAL_RUN | parser CI preparado |
| Orquestador Docker PowerShell | PENDING_REAL_RUN | código/read-back |
| Frontend image export | PASS_FROM_CACHE | imagen creada |
| Frontend clean typecheck/build | PENDING_RERUN | orquestador usa `--no-cache` |
| Backend image export | PASS_FROM_CACHE | imagen creada |
| Backend clean package | PENDING_RERUN | orquestador usa `--no-cache` |
| Maven verify/Spotless | NOT_RUN | pendiente |
| Unit tests/ArchUnit | NOT_RUN | pendiente |
| Testcontainers | NOT_RUN | pendiente |
| Flyway/Hibernate | NOT_RUN | stack no inició |
| Compose app | FAIL_FIXED_PENDING_RERUN | puertos variables |
| PostgreSQL health | NOT_RUN | pendiente |
| Backend health | NOT_RUN | pendiente |
| Frontend health | NOT_RUN | pendiente |
| Smoke PowerShell | NOT_RUN | pendiente |
| Smoke container | NOT_RUN | pendiente |
| package-lock | MISSING | generador versionado |
| Docker/CI npm ci automático | IMPLEMENTED_PENDING_LOCK | condicional |
| Scripts Unix | PASS_SYNTAX | `sh -n` disponible |
| Scripts PowerShell | PARTIAL_REAL | preflight ejecutado; parser CI preparado |
| Makefile | PASS_SYNTAX | `make -n` |
| Compose YAML | PASS_STRUCTURE | cuatro servicios/puertos variables |
| CI YAML | PASS_STRUCTURE | cuatro jobs y scripts nuevos |
| GitHub Actions | NO_VISIBLE_RUN | sin checks visibles |
| Datos sensibles | PASS_STATIC | lote real ausente |
| Envío real | PASS_BY_DESIGN | sin adaptador |

## Cambios correctivos acumulados

1. exclusiones importadas retroactivas y auditadas;
2. revisiones ambiguas persistidas en preview;
3. duplicados exactos enlazados;
4. límites 10 MB y HTTP 413;
5. orden hoja/fila;
6. validación y recuperación por fila;
7. parser CSV endurecido;
8. fechas Excel UTC;
9. preview con exclusiones;
10. `excludedRows` separado y visible;
11. filename seguro;
12. Basic Auth UTF-8;
13. DB local coherente;
14. `main` canónico;
15. perfiles app/smoke;
16. imágenes y health checks;
17. smoke host/container;
18. PowerShell corregido;
19. CI sin caché npm prematura;
20. contextos Docker seguros;
21. credenciales frontend no anulables;
22. tipos Vite/CSS;
23. generación de lockfile mediante Docker;
24. PostgreSQL host configurable;
25. backend host configurable;
26. frontend host configurable;
27. preflight con tres puertos;
28. smoke con URLs derivadas;
29. configurador conjunto de puertos;
30. wrappers retrocompatibles;
31. orquestador Docker Windows;
32. transcripts fuera de Git;
33. adopción automática de npm ci.

## Reejecución inmediata

```powershell
Set-Location C:\laburo\crm-platform
git switch main
git fetch origin
git pull --ff-only

git diff --ignore-space-at-eol -- mvnw.cmd
git restore -- mvnw.cmd

powershell -ExecutionPolicy Bypass -File scripts/validate-docker-stack.ps1 `
  -PostgresPort 55432 `
  -BackendPort 8080 `
  -FrontendPort 5173 `
  -KeepRunning
```

Puertos alternativos:

```powershell
powershell -ExecutionPolicy Bypass -File scripts/validate-docker-stack.ps1 `
  -PostgresPort 55432 `
  -BackendPort 18080 `
  -FrontendPort 15173 `
  -KeepRunning
```

Después:

```powershell
.\mvnw.cmd -B -f backend\pom.xml verify
powershell -ExecutionPolicy Bypass -File scripts/generate-frontend-lock.ps1
docker compose --progress plain --profile app build --no-cache frontend
powershell -ExecutionPolicy Bypass -File scripts/smoke-test.ps1
```

## Protocolo para PASS

1. registrar SHA y working tree;
2. restaurar cambios accidentales de `mvnw.cmd`;
3. ejecutar configurador de puertos;
4. preflight con tres puertos PASS;
5. frontend clean build PASS;
6. backend clean package PASS;
7. stack con tres servicios healthy;
8. Flyway/Hibernate PASS;
9. smoke host PASS;
10. smoke container PASS;
11. Maven verify/Spotless/tests/Testcontainers PASS;
12. generar/versionar lockfile;
13. confirmar npm ci;
14. repetir frontend, imágenes y smoke;
15. escanear datos/secretos;
16. documentar salidas;
17. cerrar SEG-001.

## Condición de cierre

No se cumple todavía. Existe progreso real y automatización suficiente para ejecutar el siguiente recorrido de forma reproducible, pero faltan los builds limpios, el stack saludable, las pruebas, las migraciones y el lockfile.
