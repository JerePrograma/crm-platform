# Validación SEG-001

## Estado

```text
PENDING_COMPLETE_LOCAL_VALIDATION
AUTOMATION_IMPLEMENTED_WINDOWS_AND_UNIX
POWERSHELL_SYNTAX_PASS
PREFLIGHT_PASS
FRONTEND_CLEAN_BUILD_PASS
BACKEND_CLEAN_IMAGE_BUILD_PASS
LATEST_REAL_RUN=FAIL_WINDOWS_HOST_PORT_BIND
STACK_HEALTH_SMOKE_PENDING
MAVEN_VERIFY_TESTCONTAINERS_PENDING
LOCKFILE_NPM_CI_PENDING
CI_NOT_VISIBLE
CONSOLIDATED_IN_MAIN
```

Una automatización implementada no equivale a una ejecución verde. Un build de imagen con `-DskipTests` tampoco sustituye `mvn verify`.

## Rama

```text
main
```

La rama histórica está detrás y no contiene cambios exclusivos.

## Evidencias versionadas

```text
docs/validation/SEG-001-static-automation-2026-07-20.md
docs/validation/SEG-001-container-build-2026-07-20.md
docs/validation/SEG-001-rerun-2026-07-20.md
docs/validation/SEG-001-local-orchestration-2026-07-20.md
docs/validation/SEG-001-complete-validation-automation-2026-07-20.md
docs/validation/SEG-001-cross-platform-validation-2026-07-20.md
docs/validation/SEG-001-powershell-parser-failure-2026-07-21.md
docs/validation/SEG-001-port-bind-failure-2026-07-21.md
```

## Ejecución real acumulada

### Intento 1 — frontend

```text
preflight: PASS
guardas: PASS
npm install: PASS
frontend TypeScript/build: FAIL
backend: CANCELED
stack/smoke: NOT_RUN
```

Se corrigieron credenciales anulables en callbacks y el import CSS sin declaración.

### Intento 2 — puerto 5432

```text
frontend image: PASS_FROM_CACHE
backend image: PASS_FROM_CACHE
clean builds: NO DEMOSTRADOS
stack: FAIL por puerto host 5432
servicios/Flyway/Hibernate/smoke: NOT_RUN
```

Después se hicieron configurables los tres puertos host.

### Intento 3 — parser PowerShell

```text
EXECUTED_FAIL — POWERSHELL_PARSE_ERROR
```

El patrón `$LASTEXITCODE:` impidió ejecutar el script. Se corrigió en tres archivos y se añadió un parser local y una regresión CI.

### Intento 4 — clean builds y puerto 55432

Checkout:

```text
main
65b64000a7e8f6abd71f2b118cebe904ee61f1d1
working tree: limpio
Docker 29.3.1
```

Resultados:

```text
PowerShell syntax: PASS — 10 scripts
preflight container-only: PASS
Compose config: PASS
frontend clean build: PASS
backend clean image build: PASS
stack: FAIL
causa: 127.0.0.1:55432 no pudo enlazarse
working tree final: limpio
```

No se ejecutaron health, Flyway, Hibernate, smoke, Maven verify, Testcontainers, package-lock ni npm ci.

## Matriz actual

| Control | Estado | Evidencia/acción |
|---|---|---|
| Consolidación en `main` | PASS | fast-forward y comparación |
| Rama histórica sin trabajo exclusivo | PASS | comparación de refs |
| Guardas de envío | PASS | preflight real |
| Sin adaptador de envío | PASS_BY_DESIGN | arquitectura |
| Datos reales fuera de Git | PASS_STATIC | reglas y revisión |
| Correcciones TypeScript | EXECUTED_PASS | clean frontend build |
| TypeScript strict | EXECUTED_PASS | `tsc -b` dentro del build |
| Vite production build | EXECUTED_PASS | clean frontend build |
| Frontend image export | EXECUTED_PASS | `--no-cache` |
| Backend dependency resolution | EXECUTED_PASS | clean image build |
| Backend package con tests omitidos | EXECUTED_PASS | Dockerfile build |
| Backend runtime image export | EXECUTED_PASS | `--no-cache` |
| Maven verify | NOT_RUN | siguiente recorrido |
| Spotless | NOT_RUN | Maven verify |
| Unit tests | NOT_RUN | Maven verify |
| ArchUnit | NOT_RUN | Maven verify |
| Testcontainers | NOT_RUN | backend verify container |
| Compose config | EXECUTED_PASS | intento 4 |
| Tres puertos configurables | PASS_CODE_REVIEW | env/Compose/scripts |
| Parser PowerShell | EXECUTED_PASS | 10 scripts |
| Preflight endurecido | EXECUTED_PASS | Docker daemon y guardas |
| Puerto PostgreSQL 55432 | EXECUTED_FAIL | ocupado o reservado en Windows |
| Checker de enlace Windows | IMPLEMENTED_NOT_RUN | `check-host-ports.ps1` |
| PostgreSQL health | NOT_RUN | stack no inició |
| Backend health | NOT_RUN | stack no inició |
| Frontend health | NOT_RUN | stack no inició |
| Flyway V1–V5 | NOT_RUN | stack no inició |
| Hibernate validate | NOT_RUN | stack no inició |
| Smoke host | NOT_RUN | stack no inició |
| Smoke contenedor | NOT_RUN | stack no inició |
| package-lock real | MISSING | fase posterior |
| npm ci real | NOT_RUN | después del lockfile |
| Seguridad final | NOT_RUN | fase posterior |
| Evidencia JSON/transcript | EXECUTED_PASS_PARTIAL | archivos locales generados |
| Working tree final | PASS | sin cambios rastreados |
| CI visible | NO_VISIBLE_RUN | conector sin estados |

## Correcciones posteriores al intento 4

1. `scripts/check-host-ports.ps1` intenta enlazar los tres puertos;
2. `validate-docker-stack.ps1` ejecuta el checker después del cleanup y antes de builds;
3. el JSON Docker registra `hostPorts`;
4. `-KeepRunning` solo marca stack conservado cuando existen contenedores en ejecución;
5. CI ejecuta el checker con puertos alternativos;
6. la próxima ejecución usa PostgreSQL en `15432` después de comprobarlo.

## Ejecución canónica siguiente

```powershell
Set-Location C:\laburo\crm-platform

git switch main
git fetch origin
git pull --ff-only
git status --short

powershell -ExecutionPolicy Bypass -File scripts/check-powershell-syntax.ps1

Get-NetTCPConnection -LocalPort 55432 -ErrorAction SilentlyContinue
netsh interface ipv4 show excludedportrange protocol=tcp

powershell -ExecutionPolicy Bypass -File scripts/check-host-ports.ps1 `
  -PostgresPort 15432 `
  -BackendPort 8080 `
  -FrontendPort 5173

powershell -ExecutionPolicy Bypass -File scripts/validate-seg001.ps1 `
  -PostgresPort 15432 `
  -BackendPort 8080 `
  -FrontendPort 5173 `
  -KeepRunning
```

## Evidencia esperada

```text
validation-output/seg001-docker-*.json
validation-output/seg001-complete-*.json
validation-output/seg001-complete-*.log
frontend/package-lock.json
```

## Protocolo después de PASS

1. revisar JSON y transcript;
2. revisar `frontend/package-lock.json` y su SHA-256;
3. confirmar que no exista otro cambio;
4. versionar únicamente el lockfile;
5. repetir desde árbol limpio;
6. confirmar `npm ci` desde el primer build;
7. observar CI;
8. actualizar esta matriz;
9. cerrar SEG-001.

## Condición de cierre

- [x] sintaxis PowerShell PASS;
- [x] preflight actualizado PASS;
- [x] clean frontend PASS;
- [x] clean backend image build PASS;
- [ ] checker de puertos alternativos PASS;
- [ ] tres servicios healthy;
- [ ] Flyway PASS;
- [ ] Hibernate validate PASS;
- [ ] smoke host PASS;
- [ ] smoke container PASS;
- [ ] Maven verify PASS;
- [ ] Spotless PASS;
- [ ] unit tests PASS;
- [ ] ArchUnit PASS;
- [ ] Testcontainers PASS;
- [ ] package-lock versionado;
- [ ] npm ci PASS con lockfile versionado;
- [ ] seguridad PASS;
- [ ] evidencia final registrada;
- [ ] CI visible verde o excepción documentada;
- [ ] SEG-001 COMPLETE;
- [ ] SEG-002 ACTIVE.

## Seguridad

```text
SENDING_ENABLED=false
SENDING_DRY_RUN=true
SENDING_DAILY_LIMIT=0
SENDING_KILL_SWITCH=true
```

El intento 4 no inició servicios, no modificó datos y no produjo comunicaciones.
