# Validación SEG-001

## Estado

```text
PENDING_COMPLETE_LOCAL_VALIDATION
AUTOMATION_IMPLEMENTED
CLEAN_BUILDS_PENDING
LOCKFILE_PENDING
CI_NOT_VISIBLE
CONSOLIDATED_IN_MAIN
```

Una automatización implementada no equivale a una ejecución verde. Una imagen exportada desde caché no equivale a un build limpio.

## Rama

```text
main
```

## Evidencias versionadas

```text
docs/validation/SEG-001-static-automation-2026-07-20.md
docs/validation/SEG-001-container-build-2026-07-20.md
docs/validation/SEG-001-rerun-2026-07-20.md
docs/validation/SEG-001-local-orchestration-2026-07-20.md
docs/validation/SEG-001-complete-validation-automation-2026-07-20.md
```

## Evidencia real acumulada

### Entorno aportado

```text
Windows PowerShell
Docker 29.3.1 build c2be9cc
modo container-only
```

### Primer intento

```text
preflight: PASS
guardas: PASS
npm install: PASS
frontend TypeScript/build: FAIL
backend: CANCELED
stack: NOT_RUN
smoke: NOT_RUN
```

Errores:

```text
Credentials | null en getProspect
Credentials | null en refresh
import CSS sin declaración Vite/TypeScript
```

Correcciones:

```text
72f0421caaf4898ce04fd97724ecbad9a4ed6390
31960db073d6df0cae683b02267a752b9538e08f
```

### Segunda reejecución

```text
frontend image: PASS_FROM_CACHE
backend image: PASS_FROM_CACHE
frontend clean build: NO DEMOSTRADO
backend clean build: NO DEMOSTRADO
stack: FAIL
causa: puerto host 5432 no disponible
postgres/backend/frontend: NOT_STARTED
Flyway/Hibernate/smoke: NOT_RUN
```

## Hardening implementado después de la evidencia real

### Puertos

```text
POSTGRES_HOST_PORT
BACKEND_HOST_PORT
FRONTEND_HOST_PORT
```

- enteros entre 1 y 65535;
- valores distintos;
- publicación en loopback;
- `DATABASE_URL` coordinada;
- red interna sin cambios.

### Stack y evidencia

- validador Docker PowerShell;
- builds sin caché por defecto;
- health por servicio;
- smoke host y contenedor;
- transcript local;
- resumen JSON;
- parámetro `-NoTranscript` para composición.

### Backend verify contenedorizado

- Maven/Java 21 en Docker;
- repositorio en solo lectura;
- target efímero;
- caché Maven;
- socket Docker;
- Testcontainers host override;
- limpieza final.

### Lockfile y npm ci

- generación `--package-lock-only`;
- lifecycle scripts deshabilitados;
- `node_modules` prohibido;
- SHA-256 del lockfile;
- Dockerfile/CI/Makefile eligen npm ci cuando existe.

### Validador integral

```text
scripts/validate-seg001.ps1
```

Encadena:

1. rama/árbol limpio;
2. Docker stack limpio;
3. Maven verify/Testcontainers;
4. package-lock;
5. npm ci;
6. recreación frontend;
7. smoke final;
8. seguridad;
9. evidencia JSON/transcript.

### Seguridad centralizada

```text
scripts/check-repository-safety.ps1
scripts/check-repository-safety.sh
```

Bloquea entorno local, evidencia, datos privados, lote operativo, claves/certificados y JSON de credenciales rastreados.

## Matriz actual

| Control | Estado | Evidencia/acción |
|---|---|---|
| Consolidación en main | PASS | fast-forward y comparación |
| Rama histórica sin cambios exclusivos | PASS | compare main vs feature |
| Guardas de envío | PASS | preflight real |
| Sin adaptador de envío | PASS_BY_DESIGN | revisión de arquitectura |
| Datos reales fuera de Git | PASS_STATIC | revisión y reglas |
| Correcciones TypeScript | PASS_CODE_REVIEW | commits/read-back |
| `excludedRows` visible | PASS_CODE_REVIEW | UI `Bloqueadas` |
| Compose cuatro servicios | PASS_STRUCTURE | YAML |
| Tres puertos configurables | PASS_CODE_REVIEW | env/Compose/scripts |
| Puertos válidos/distintos | PASS_CODE_REVIEW | preflight |
| Configurador Unix | PASS_FUNCTIONAL_ISOLATED | `.env` temporal |
| Configurador PowerShell | PENDING_REAL_RUN | parser CI preparado |
| Preflight PowerShell inicial | PASS | salida aportada |
| Preflight tres puertos | PENDING_RERUN | validador integral |
| Frontend image cacheada | PASS_FROM_CACHE | imagen exportada |
| Frontend clean build | PENDING_RERUN | validador integral |
| Backend image cacheada | PASS_FROM_CACHE | imagen exportada |
| Backend clean package | PENDING_RERUN | validador integral |
| PostgreSQL health | NOT_RUN | validador integral |
| Backend health | NOT_RUN | validador integral |
| Frontend health | NOT_RUN | validador integral |
| Flyway arranque | NOT_RUN | logs/health |
| Hibernate validate arranque | NOT_RUN | logs/health |
| Smoke host | NOT_RUN | validador integral |
| Smoke container | NOT_RUN | validador integral |
| Maven verify | NOT_RUN | backend verify container |
| Spotless | NOT_RUN | Maven verify |
| Unit tests | NOT_RUN | Maven verify |
| ArchUnit | NOT_RUN | Maven verify |
| Testcontainers | NOT_RUN | backend verify container |
| Backend verify shell syntax | PASS_SYNTAX | `sh -n` |
| Backend verify PowerShell | PENDING_PARSER | CI parser |
| Lockfile generator shell | PASS_SYNTAX | `sh -n` |
| Lockfile generator PowerShell | PENDING_PARSER | CI parser |
| package-lock real | MISSING | validador integral |
| npm ci real | NOT_RUN | después de lockfile |
| Validador Docker JSON | IMPLEMENTED_NOT_RUN | script/read-back |
| Validador integral | IMPLEMENTED_NOT_RUN | script/read-back |
| Seguridad shell | IMPLEMENTED_PENDING_RUN | CI/Make |
| Seguridad PowerShell | IMPLEMENTED_PENDING_RUN | validador integral |
| Makefile | PASS_PARSE | targets expandidos |
| CI YAML | PASS_PARSE | cuatro jobs |
| CI scripts nuevos | PASS_STRUCTURE | listas parser/shell |
| GitHub Actions visible | NO_VISIBLE_RUN | conector devuelve vacío |

## Controles estáticos ejecutados más recientes

```text
verify-backend-container.sh: PASS_SYNTAX
generate-frontend-lock.sh: PASS_SYNTAX
Make backend-verify-container: PASS_PARSE
Make verify-container: PASS_PARSE
CI YAML: PASS_PARSE
CI safety step: PASS_STRUCTURE
```

La primera ejecución de `make -n` se realizó desde un directorio distinto al Makefile y la recursión no encontró `smoke-container`. Repetida desde la raíz correcta, el parseo pasó. No fue un defecto del Makefile versionado.

## Cambios correctivos acumulados

1. exclusiones importadas retroactivas y auditadas;
2. revisiones ambiguas persistidas en preview;
3. duplicados exactos enlazados;
4. límite 10 MB y HTTP 413;
5. orden hoja/fila;
6. validación y recuperación por fila;
7. parser CSV endurecido;
8. fechas Excel UTC;
9. preview con exclusiones;
10. `excludedRows` separado y visible;
11. filename seguro;
12. Basic Auth UTF-8;
13. DB local coherente;
14. `main` canónico;
15. perfiles app/smoke;
16. imágenes y health checks;
17. smoke host/container;
18. PowerShell corregido;
19. CI sin caché npm prematura;
20. contextos Docker seguros;
21. credenciales frontend no anulables;
22. tipos Vite/CSS;
23. generación de lockfile mediante Docker;
24. tres puertos host configurables;
25. preflight coordinado;
26. evidencia JSON estructurada;
27. Maven verify/Testcontainers contenedorizado;
28. lockfile sin lifecycle scripts;
29. validador integral;
30. seguridad centralizada del repositorio;
31. Make/CI ampliados.

## Ejecución canónica pendiente

```powershell
Set-Location C:\laburo\crm-platform
git switch main
git pull --ff-only
git restore -- mvnw.cmd

powershell -ExecutionPolicy Bypass -File scripts/validate-seg001.ps1 `
  -PostgresPort 55432 `
  -BackendPort 8080 `
  -FrontendPort 5173 `
  -KeepRunning
```

## Evidencia esperada

```text
validation-output/seg001-docker-*.json
validation-output/seg001-complete-*.json
validation-output/seg001-complete-*.log
frontend/package-lock.json
```

## Protocolo después de PASS

1. revisar JSON;
2. revisar transcript;
3. revisar package-lock;
4. confirmar que no exista otro cambio;
5. versionar package-lock;
6. repetir validador desde árbol limpio;
7. confirmar npm ci desde el inicio;
8. observar CI;
9. resumir resultados aquí;
10. cerrar SEG-001.

## Condición de cierre

- [ ] clean frontend PASS;
- [ ] clean backend PASS;
- [ ] PostgreSQL/backend/frontend healthy;
- [ ] Flyway PASS;
- [ ] Hibernate validate PASS;
- [ ] smoke host PASS;
- [ ] smoke container PASS;
- [ ] Maven verify PASS;
- [ ] Spotless PASS;
- [ ] unit tests PASS;
- [ ] ArchUnit PASS;
- [ ] Testcontainers PASS;
- [ ] package-lock versionado;
- [ ] npm ci PASS con lock versionado;
- [ ] seguridad PASS;
- [ ] evidencia registrada;
- [ ] CI visible verde o excepción explícita documentada;
- [ ] SEG-001 COMPLETE;
- [ ] SEG-002 ACTIVE.

## Seguridad

```text
SENDING_ENABLED=false
SENDING_DRY_RUN=true
SENDING_DAILY_LIMIT=0
SENDING_KILL_SWITCH=true
```

Ningún control despliega, envía mensajes o importa el lote real.
