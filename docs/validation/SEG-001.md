# Validación SEG-001

## Estado

`PENDING_RERUN — REAL_FRONTEND_FAILURE_FIXED — CONSOLIDATED_IN_MAIN`

Una prueba versionada no equivale a una prueba aprobada. Un fallo corregido tampoco equivale a una reejecución verde.

## Rama

```text
main
```

## Consolidación

- fecha: `2026-07-20`;
- main anterior: `7fca32b9b777f8b8c5c305c0699d14877babebc6`;
- árbol consolidado: `1c089cd19ce953e93531c1ef3c54f79d28575444`;
- fast-forward `force=false`;
- comparación inmediata idéntica;
- comparación posterior: rama histórica detrás y sin cambios exclusivos;
- evidencia: `docs/main-consolidation.md`.

## Evidencias disponibles

### Revisión estática y automatización

```text
docs/validation/SEG-001-static-automation-2026-07-20.md
```

### Primer build contenedorizado real

```text
docs/validation/SEG-001-container-build-2026-07-20.md
```

## Primera ejecución real

Entorno:

```text
Windows PowerShell
Docker 29.3.1 build c2be9cc
modo container-only
```

Preflight:

```text
PASS
DATABASE_URL presente
bootstrap configurado
SENDING_ENABLED=false
SENDING_DRY_RUN=true
SENDING_DAILY_LIMIT=0
SENDING_KILL_SWITCH=true
```

Build:

```text
postgres image pull: PASS
base image metadata/layers: PASS
frontend npm install: PASS (24 paquetes)
frontend TypeScript/build: FAIL
backend build: CANCELED
Flyway/Hibernate: NOT_RUN
stack health: NOT_RUN
smoke: NOT_RUN
```

## Errores reproducidos

```text
src/App.tsx: Credentials | null no asignable a Credentials en getProspect
src/App.tsx: Credentials | null no asignable a Credentials en refresh
src/main.tsx: falta declaración para import lateral de ./styles.css
```

## Correcciones versionadas

### `72f0421caaf4898ce04fd97724ecbad9a4ed6390`

- referencia `activeCredentials` no anulable después del guard;
- callbacks y paneles reciben credenciales no anulables;
- `strict: true` permanece activo;
- resumen muestra `excludedRows` como `Bloqueadas`.

### `31960db073d6df0cae683b02267a752b9538e08f`

- `frontend/src/vite-env.d.ts`;
- referencia `vite/client`;
- declaración `*.css`.

Read-back remoto de ambos cambios: `PASS`.

## Matriz actual

| Control | Estado | Evidencia |
|---|---|---|
| Consolidación en main | PASS | Fast-forward y comparación |
| Configuración DB | PASS_STATIC | `.env`/Compose/backend coherentes |
| Documentación | PASS_STATIC | README y guías |
| `.dockerignore` backend/frontend | PASS_STATIC | datos/secretos excluidos |
| `.gitattributes` | PASS_STATIC | EOL multiplataforma |
| Compose YAML | PASS_SYNTAX | cuatro servicios |
| CI YAML | PASS_SYNTAX | cuatro jobs y smoke E2E |
| Scripts Unix | PASS_SYNTAX | `sh -n` |
| Makefile | PASS_SYNTAX | `make -n` incluido smoke-container |
| Preflight PowerShell container-only | PASS | salida aportada por ejecución real |
| Guardas de envío | PASS | cuatro valores seguros confirmados |
| Pull de PostgreSQL | PASS | imagen descargada |
| Pull de bases frontend/backend | PARTIAL_PASS | metadata y capas descargadas |
| npm install frontend | PASS | 24 paquetes instalados en stage |
| Frontend typecheck/build antes de fix | FAIL | tres errores TypeScript |
| Corrección nullability | PASS_CODE_REVIEW | commit y read-back |
| Declaración Vite/CSS | PASS_CODE_REVIEW | commit y read-back |
| UI `excludedRows` | PASS_CODE_REVIEW | control `Bloqueadas` visible en código |
| Frontend typecheck/build después de fix | PENDING_RERUN | reejecución requerida |
| PowerShell syntax formal | IMPLEMENTED_NOT_RUN | parser CI preparado |
| Maven/Spotless | NOT_REACHED | backend cancelado |
| Unit tests/ArchUnit | NOT_REACHED | backend cancelado |
| Testcontainers | NOT_REACHED | backend cancelado |
| Flyway/Hibernate | NOT_REACHED | backend no iniciado |
| package-lock | MISSING | generar/versionar |
| `docker compose config` registrado | PENDING | ejecutar y guardar salida |
| imagen backend | CANCELED | build paralelo cancelado |
| imagen frontend | FAIL_FIXED_PENDING_RERUN | errores corregidos |
| stack app | NOT_RUN | imágenes incompletas |
| smoke E2E container | NOT_RUN | stack no disponible |
| smoke host PowerShell | NOT_RUN | stack no disponible |
| GitHub Actions | NO_VISIBLE_RUN | sin statuses/runs visibles |
| datos sensibles | PASS_STATIC | sin lote real |
| envío real ausente | PASS_BY_DESIGN | sin adaptador |

## CI implementado

### backend

- Maven verify;
- Spotless;
- unit tests;
- ArchUnit;
- Testcontainers.

### frontend

- npm install;
- typecheck;
- build.

### scripts

- sintaxis Unix;
- sintaxis PowerShell;
- preflight fail-closed.

### compose-images-and-smoke

- `.env` ficticio fail-closed;
- Compose perfiles app/smoke;
- build backend;
- build frontend;
- arranque PostgreSQL/backend/frontend;
- contenedor smoke;
- health + Basic Auth + frontend;
- logs en fallo;
- cleanup con volumen de CI.

Caché npm desactivada hasta lockfile.

## Correcciones acumuladas relevantes

1. exclusiones importadas retroactivas/auditadas;
2. ambigüedades persistidas en preview;
3. duplicados exactos enlazados;
4. límites 10 MB/HTTP 413;
5. orden hoja/fila;
6. validación y recuperación por fila;
7. CSV endurecido;
8. fechas UTC;
9. preview con exclusiones;
10. `excludedRows` separado y visible;
11. filename seguro;
12. Basic Auth UTF-8;
13. DB local coherente;
14. main canónico;
15. stack app/smoke;
16. imágenes y health checks;
17. smoke host/container;
18. PowerShell corregido;
19. CI sin caché npm prematura;
20. build contexts seguros;
21. credenciales no anulables en render autenticado;
22. tipos Vite/CSS declarados.

## Reejecución inmediata

```powershell
Set-Location C:\laburo\crm-platform
git switch main
git pull --ff-only
git rev-parse HEAD

powershell -ExecutionPolicy Bypass -File scripts/preflight.ps1 -ContainerOnly

docker compose --profile app build frontend --progress=plain

docker compose --profile app up -d --build

docker compose --profile app ps
```

Si el stack queda saludable:

```powershell
powershell -ExecutionPolicy Bypass -File scripts/smoke-test.ps1
```

Para aislar el backend si aparece otro fallo:

```powershell
docker compose --profile app build backend --progress=plain
```

## Lockfile

Generar sin Node local:

```powershell
docker run --rm `
  -v "${PWD}\frontend:/workspace/frontend" `
  -w /workspace/frontend `
  node:22-alpine `
  npm install --no-audit --no-fund
```

Después:

- verificar `frontend/package-lock.json`;
- versionarlo;
- migrar CI/Dockerfile a `npm ci`;
- repetir frontend, imágenes y smoke.

## Protocolo para PASS

1. actualizar main y registrar SHA;
2. repetir preflight;
3. reconstruir frontend corregido;
4. compilar backend;
5. ejecutar Maven/Spotless/tests;
6. confirmar Flyway/Hibernate/Testcontainers;
7. generar lockfile;
8. ejecutar npm ci/typecheck/build;
9. validar Compose app/smoke;
10. construir ambas imágenes;
11. ejecutar stack y smoke E2E;
12. escanear datos/secretos;
13. corregir cualquier nuevo fallo con regresión;
14. repetir matriz completa;
15. registrar comandos y salidas;
16. cerrar SEG-001 solo con controles principales en PASS.

## Condición de cierre

No se cumple todavía. El preflight real pasa y los primeros errores de frontend están corregidos, pero falta la reejecución y toda la evidencia backend/E2E.