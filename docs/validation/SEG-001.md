# Validación SEG-001

## Estado

`PENDING_CLEAN_RERUN — CACHED_IMAGES_EXPORTED — POSTGRES_HOST_PORT_FIXED — CONSOLIDATED_IN_MAIN`

Una imagen exportada desde caché no equivale a una compilación limpia. Un bloqueo de puerto corregido tampoco equivale a un stack saludable.

## Rama

```text
main
```

## Evidencias

### Automatización estática

```text
docs/validation/SEG-001-static-automation-2026-07-20.md
```

### Primer build real

```text
docs/validation/SEG-001-container-build-2026-07-20.md
```

### Reejecución, caché y conflicto de puerto

```text
docs/validation/SEG-001-rerun-2026-07-20.md
```

## Ejecución real acumulada

Entorno:

```text
Windows PowerShell
Docker 29.3.1 build c2be9cc
modo container-only
```

### Preflight

```text
PASS
bootstrap configurado
SENDING_ENABLED=false
SENDING_DRY_RUN=true
SENDING_DAILY_LIMIT=0
SENDING_KILL_SWITCH=true
```

### Primer build frontend

```text
npm install: PASS
24 paquetes instalados
TypeScript/build: FAIL
```

Errores:

```text
Credentials | null en getProspect
Credentials | null en refresh
import CSS sin declaración Vite/TypeScript
```

Correcciones:

```text
72f0421caaf4898ce04fd97724ecbad9a4ed6390
31960db073d6df0cae683b02267a752b9538e08f
```

### Segunda exportación de imágenes

Frontend:

```text
imagen exportada: PASS
npm run build: CACHED
compilación limpia: NO DEMOSTRADA
```

Backend:

```text
imagen exportada: PASS
mvn package: CACHED
compilación limpia: NO DEMOSTRADA
pruebas: NO EJECUTADAS
```

### Arranque

```text
docker compose --profile app up -d: FAIL
```

Error:

```text
127.0.0.1:5432 no disponible en Windows
```

Consecuencias:

```text
postgres: NOT_STARTED
backend: NOT_STARTED
frontend: NOT_STARTED
Flyway: NOT_RUN
Hibernate: NOT_RUN
smoke: NOT_RUN
```

### Corrección de puerto

Versionado en `main`:

```text
POSTGRES_HOST_PORT=55432
DATABASE_URL=jdbc:postgresql://localhost:55432/gestudio_crm
Compose: 127.0.0.1:${POSTGRES_HOST_PORT}:5432
backend interno: postgres:5432
```

Preflight Unix/PowerShell valida puerto, rango, URL y guardas.

## Matriz actual

| Control | Estado | Evidencia |
|---|---|---|
| Consolidación en main | PASS | fast-forward y comparación |
| Guardas de envío | PASS | preflight real |
| Preflight PowerShell inicial | PASS | salida aportada |
| Correcciones TypeScript | PASS_CODE_REVIEW | commits y read-back |
| `excludedRows` visible | PASS_CODE_REVIEW | UI `Bloqueadas` |
| Configuración puerto PostgreSQL | PASS_CODE_REVIEW | Compose/env/preflight |
| Frontend image export | PASS_FROM_CACHE | imagen creada |
| Frontend clean typecheck/build | PENDING_RERUN | usar `--no-cache` |
| Backend image export | PASS_FROM_CACHE | imagen creada |
| Backend clean package | PENDING_RERUN | usar `--no-cache` |
| Maven verify/Spotless | NOT_RUN | pendiente |
| Unit tests/ArchUnit | NOT_RUN | pendiente |
| Testcontainers | NOT_RUN | pendiente |
| Flyway/Hibernate | NOT_RUN | stack no inició |
| Compose app | FAIL_FIXED_PENDING_RERUN | puerto 5432 corregido |
| PostgreSQL health | NOT_RUN | pendiente puerto 55432 |
| Backend health | NOT_RUN | pendiente |
| Frontend health | NOT_RUN | pendiente |
| Smoke PowerShell | NOT_RUN | pendiente |
| Smoke container | NOT_RUN | pendiente |
| package-lock | MISSING | generador versionado |
| npm ci | NOT_IMPLEMENTED_YET | depende del lockfile |
| Scripts Unix | PASS_SYNTAX | `sh -n` |
| Scripts PowerShell | PARTIAL_REAL | preflight ejecutado; parser CI preparado |
| Makefile | PASS_SYNTAX | `make -n` |
| GitHub Actions | NO_VISIBLE_RUN | sin checks visibles |
| Datos sensibles | PASS_STATIC | lote real ausente |
| Envío real | PASS_BY_DESIGN | sin adaptador |

## Cambios correctivos acumulados

1. exclusiones importadas retroactivas y auditadas;
2. revisiones ambiguas persistidas en preview;
3. duplicados exactos enlazados;
4. límites 10 MB y HTTP 413;
5. orden hoja/fila;
6. validación y recuperación por fila;
7. parser CSV endurecido;
8. fechas Excel UTC;
9. preview con exclusiones;
10. `excludedRows` separado y visible;
11. filename seguro;
12. Basic Auth UTF-8;
13. DB local coherente;
14. `main` canónico;
15. perfiles app/smoke;
16. imágenes y health checks;
17. smoke host/container;
18. scripts PowerShell corregidos;
19. CI sin caché npm prematura;
20. contextos Docker seguros;
21. credenciales frontend no anulables;
22. tipos Vite/CSS;
23. generación de lockfile mediante Docker;
24. puerto PostgreSQL host configurable;
25. preflight con validación de puerto/URL.

## Reejecución inmediata

Actualizar:

```powershell
Set-Location C:\laburo\crm-platform
git switch main
git pull --ff-only
git rev-parse HEAD
```

Revisar modificación local:

```powershell
git diff -- mvnw.cmd
# si no es intencional:
git restore -- mvnw.cmd
```

Editar `.env`:

```dotenv
POSTGRES_HOST_PORT=55432
DATABASE_URL=jdbc:postgresql://localhost:55432/gestudio_crm
```

Validar y limpiar contenedores incompletos:

```powershell
powershell -ExecutionPolicy Bypass -File scripts/preflight.ps1 -ContainerOnly
docker compose --profile app --profile smoke down --remove-orphans
```

Builds limpios:

```powershell
docker compose --progress plain --profile app build --no-cache frontend
docker compose --progress plain --profile app build --no-cache backend
```

Stack y smoke:

```powershell
docker compose --profile app up -d
docker compose --profile app ps
powershell -ExecutionPolicy Bypass -File scripts/smoke-test.ps1
```

Pruebas:

```powershell
.\mvnw.cmd -B -f backend\pom.xml verify
powershell -ExecutionPolicy Bypass -File scripts/generate-frontend-lock.ps1
```

## Protocolo para PASS

1. registrar SHA y working tree;
2. confirmar `.env` con puerto 55432 y guardas cerradas;
3. preflight PASS;
4. frontend clean build PASS;
5. backend clean package PASS;
6. stack con tres servicios healthy;
7. Flyway/Hibernate PASS;
8. smoke host PASS;
9. smoke container PASS;
10. Maven verify/Spotless/tests/Testcontainers PASS;
11. generar/versionar lockfile;
12. migrar a npm ci;
13. repetir imágenes/smoke;
14. escanear datos/secretos;
15. documentar salidas;
16. cerrar SEG-001.

## Condición de cierre

No se cumple todavía. Existe progreso real: preflight pasó, los errores TypeScript fueron corregidos y ambas imágenes pudieron exportarse. Falta demostrar builds limpios, resolver localmente el puerto mediante la nueva configuración, arrancar el stack y completar pruebas/smoke.
