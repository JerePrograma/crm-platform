# Validación SEG-001

## Estado

```text
PENDING_COMPLETE_LOCAL_VALIDATION
AUTOMATION_IMPLEMENTED_WINDOWS_AND_UNIX
POWERSHELL_SYNTAX_PASS_ON_F903
PREFLIGHT_PASS_ON_F903
FRONTEND_CLEAN_BUILD_PASS
BACKEND_CLEAN_IMAGE_BUILD_PASS
LATEST_REAL_RUN=FAIL_DOCKER_HOST_PORT_ALREADY_ALLOCATED
DOCKER_PORT_OWNER_HARDENING_IMPLEMENTED_NOT_RUN
LOCKFILE_PENDING
CI_NOT_VISIBLE
CONSOLIDATED_IN_MAIN
```

Una automatización implementada no equivale a una ejecución verde. Un `mvn package -DskipTests` exitoso dentro del Dockerfile no equivale a `mvn verify`.

## Rama

```text
main
```

La rama histórica está detrás y no contiene trabajo exclusivo.

## Evidencias versionadas

```text
docs/validation/SEG-001-static-automation-2026-07-20.md
docs/validation/SEG-001-container-build-2026-07-20.md
docs/validation/SEG-001-rerun-2026-07-20.md
docs/validation/SEG-001-local-orchestration-2026-07-20.md
docs/validation/SEG-001-complete-validation-automation-2026-07-20.md
docs/validation/SEG-001-cross-platform-validation-2026-07-20.md
docs/validation/SEG-001-powershell-parser-failure-2026-07-21.md
docs/validation/SEG-001-port-bind-failure-2026-07-21.md
docs/validation/SEG-001-docker-port-owner-failure-2026-07-21.md
```

## Ejecución real acumulada

### Intento 1 — frontend

```text
preflight: PASS
guardas: PASS
npm install: PASS
frontend TypeScript/build: FAIL
backend: CANCELED
stack: NOT_RUN
smoke: NOT_RUN
```

Errores corregidos:

- credenciales anulables en callbacks;
- import CSS sin declaración.

### Intento 2 — Compose

```text
frontend image: PASS_FROM_CACHE
backend image: PASS_FROM_CACHE
clean builds: NO DEMOSTRADOS
stack: FAIL por puerto host 5432
servicios: NOT_STARTED
Flyway/Hibernate/smoke: NOT_RUN
```

Hardening posterior:

- tres puertos configurables;
- URL DB coordinada;
- publicación loopback;
- preflight de daemon y Compose.

### Intento 3 — parser PowerShell

```text
scripts/validate-seg001.ps1
InvalidVariableReferenceWithDrive
$LASTEXITCODE:
```

Estado:

```text
EXECUTED_FAIL — POWERSHELL_PARSE_ERROR
```

Hardening posterior:

- tres scripts corregidos;
- checker de sintaxis PowerShell;
- regresión CI;
- normalización de `mvnw.cmd`.

### Intento 4 — rango excluido Windows

```text
PowerShell syntax: PASS
preflight: PASS
frontend clean build: PASS
backend clean image build: PASS
stack: FAIL al enlazar 127.0.0.1:55432
```

Windows confirmó que `55432` pertenecía a un rango excluido. Se añadió `check-host-ports.ps1` con `TcpListener` y diagnóstico de rangos.

### Intento 5 — publicación Docker en 15432

Commit ejecutado:

```text
f903a9e1278697af53e0bcbee3bd10b16e10b991
```

Comando:

```powershell
powershell -ExecutionPolicy Bypass -File scripts/validate-seg001.ps1 `
  -PostgresPort 15432 `
  -BackendPort 8080 `
  -FrontendPort 5173 `
  -KeepRunning
```

Aprobó:

```text
main actualizado y working tree limpio
PowerShell syntax, 11 scripts
preflight container-only
Docker daemon
Compose config
guardas de envío
TcpListener Windows para 15432, 8080 y 5173
frontend clean build --no-cache
TypeScript strict
Vite production build
backend clean image build --no-cache
Maven package con tests omitidos
```

Primer fallo real:

```text
Bind for 0.0.0.0:15432 failed: port is already allocated
```

Estado:

```text
EXECUTED_FAIL — DOCKER_HOST_PORT_ALREADY_ALLOCATED
```

No hubo contenedores del proyecto en ejecución al terminar, aunque el orquestador exterior todavía informó incorrectamente `Stack left running`. Ese mensaje también fue corregido después de la evidencia.

## Matriz actual

| Control | Estado | Evidencia/acción |
|---|---|---|
| Consolidación en `main` | PASS | fast-forward y continuidad |
| Rama histórica sin trabajo exclusivo | PASS | comparación de refs |
| Working tree inicial | PASS | intento 5 |
| Guardas de envío | PASS | preflight real |
| Sin adaptador de envío | PASS_BY_DESIGN | arquitectura |
| Datos reales fuera de Git | PASS_STATIC | reglas y revisión |
| Correcciones TypeScript | PASS | clean build intento 5 |
| TypeScript strict | PASS | `tsc -b` intento 5 |
| Vite production build | PASS | intento 5 |
| Compose cuatro servicios | PASS_STRUCTURE | YAML |
| Tres puertos configurables | PASS | scripts/env/Compose |
| Configurador PowerShell | PASS | intento 5 |
| Configurador Unix | PASS_FUNCTIONAL_ISOLATED | entorno temporal |
| Preflight actualizado | PASS | intento 5 |
| Docker daemon | PASS | intento 5 |
| PowerShell syntax | PASS_ON_F903 | 11 scripts |
| Checker Windows `TcpListener` | PASS_ON_F903 | 15432/8080/5173 |
| Checker de publicaciones Docker | IMPLEMENTED_NOT_RUN | ejecutar tras pull |
| PostgreSQL antes de builds | IMPLEMENTED_NOT_RUN | nuevo orden |
| Frontend clean image build | PASS | `--no-cache`, intento 5 |
| Backend clean image build | PASS | `--no-cache`, intento 5 |
| Maven package con tests omitidos | PASS_PARTIAL | Dockerfile |
| PostgreSQL Docker publication | EXECUTED_FAIL | 15432 ya asignado |
| PostgreSQL health | NOT_RUN | publicación falló |
| Backend startup/health | NOT_RUN | PostgreSQL no inició |
| Frontend startup/health | NOT_RUN | PostgreSQL no inició |
| Flyway startup | NOT_RUN | stack pendiente |
| Hibernate validate startup | NOT_RUN | stack pendiente |
| Smoke host | NOT_RUN | stack pendiente |
| Smoke contenedor | NOT_RUN | stack pendiente |
| Maven verify | NOT_RUN | fase posterior |
| Spotless | NOT_RUN | Maven verify |
| Unit tests | NOT_RUN | Maven verify |
| ArchUnit | NOT_RUN | Maven verify |
| Testcontainers | NOT_RUN | Maven verify |
| package-lock real | MISSING | validador pendiente |
| npm ci real | NOT_RUN | después del lockfile |
| Seguridad final | NOT_RUN_REAL | fase final |
| JSON Docker | GENERATED_FAIL | intento 5 |
| JSON/transcript integral | GENERATED_FAIL | intento 5 |
| `stackKeptRunning` exterior veraz | IMPLEMENTED_NOT_RUN | corregido después |
| CI visible | NO_VISIBLE_RUN | conector sin estados |

## Cambios correctivos acumulados

1. exclusiones retroactivas y auditadas;
2. revisiones ambiguas en preview;
3. duplicados exactos enlazados;
4. límite 10 MB y HTTP 413;
5. orden hoja/fila;
6. recuperación por fila;
7. parser CSV endurecido;
8. fechas Excel UTC;
9. preview con exclusiones;
10. métrica `Bloqueadas`;
11. filenames seguros;
12. Basic Auth UTF-8;
13. DB local coherente;
14. `main` canónico;
15. perfiles app/smoke;
16. imágenes y health checks;
17. smoke host/contenedor;
18. contextos Docker seguros;
19. nullability frontend;
20. tipos Vite/CSS;
21. lockfile mediante Docker;
22. puertos host configurables;
23. preflight fail-fast;
24. evidencia JSON;
25. Maven/Testcontainers contenedorizado;
26. lockfile sin lifecycle scripts;
27. UID/GID preservado en Unix;
28. validadores integrales Windows/Unix;
29. seguridad centralizada;
30. corrección de interpolación PowerShell;
31. checker de sintaxis local;
32. regresión CI;
33. normalización de `mvnw.cmd`;
34. checker Windows de rangos excluidos;
35. detección de publicaciones Docker;
36. PostgreSQL real antes de builds;
37. diagnóstico `docker ps` en fallos;
38. `stackKeptRunning` veraz en ambos validadores.

## Ejecución canónica siguiente

```powershell
Set-Location C:\laburo\crm-platform

git switch main
git fetch origin
git pull --ff-only

git status --short

docker ps --format "table {{.ID}}\t{{.Names}}\t{{.Ports}}"
docker ps --filter publish=15432 --format "table {{.ID}}\t{{.Names}}\t{{.Ports}}"

docker compose --profile app --profile smoke down --remove-orphans

powershell -ExecutionPolicy Bypass -File scripts/check-powershell-syntax.ps1
powershell -ExecutionPolicy Bypass -File scripts/check-host-ports.ps1 `
  -PostgresPort 15432 `
  -BackendPort 8080 `
  -FrontendPort 5173

powershell -ExecutionPolicy Bypass -File scripts/validate-seg001.ps1 `
  -PostgresPort 15432 `
  -BackendPort 8080 `
  -FrontendPort 5173 `
  -KeepRunning
```

Si `15432` está publicado por otro contenedor, detener solo ese contenedor o validar `25432` y usarlo en ambos comandos.

## Evidencia esperada

```text
validation-output/seg001-docker-*.json
validation-output/seg001-complete-*.json
validation-output/seg001-complete-*.log
frontend/package-lock.json
```

## Protocolo después de PASS

1. revisar JSON;
2. revisar transcript;
3. revisar lockfile;
4. versionar únicamente lockfile;
5. repetir desde árbol limpio;
6. confirmar npm ci desde el primer build;
7. observar CI;
8. actualizar esta matriz;
9. cerrar SEG-001.

## Condición de cierre

- [x] clean frontend build;
- [x] clean backend image build;
- [ ] PowerShell syntax después del hardening nuevo;
- [ ] checker Docker/Windows actualizado;
- [ ] PostgreSQL publication/health;
- [ ] backend/frontend healthy;
- [ ] Flyway;
- [ ] Hibernate validate;
- [ ] smoke host;
- [ ] smoke container;
- [ ] Maven verify;
- [ ] Spotless;
- [ ] unit tests;
- [ ] ArchUnit;
- [ ] Testcontainers;
- [ ] package-lock versionado;
- [ ] npm ci con lockfile versionado;
- [ ] seguridad final;
- [ ] evidencia final;
- [ ] CI visible verde o excepción documentada;
- [ ] SEG-001 COMPLETE;
- [ ] SEG-002 ACTIVE.

## Seguridad

```text
SENDING_ENABLED=false
SENDING_DRY_RUN=true
SENDING_DAILY_LIMIT=0
SENDING_KILL_SWITCH=true
```

Ningún intento ejecutó comunicaciones, despliegues o importación del XLSX real.
