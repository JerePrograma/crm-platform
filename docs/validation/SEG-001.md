# Validación SEG-001

## Estado

`PENDING_EXECUTION — STATIC_AUTOMATION_PARTIAL_PASS — CONSOLIDATED_IN_MAIN`

Una prueba versionada no equivale a una prueba aprobada.

## Rama

```text
main
```

## Consolidación

- fecha: `2026-07-20`;
- main anterior: `7fca32b9b777f8b8c5c305c0699d14877babebc6`;
- árbol consolidado: `1c089cd19ce953e93531c1ef3c54f79d28575444`;
- fast-forward `force=false`;
- comparación inmediata idéntica;
- comparación posterior: rama histórica detrás y sin cambios exclusivos;
- evidencia: `docs/main-consolidation.md`.

## Última consulta de GitHub Actions registrada

Commit consultado:

```text
f2cddf2c9ca37416470d167e16b4a552d2839037
```

Resultado:

```text
combined statuses: []
workflow runs visibles: []
```

Registrar un nuevo SHA en la próxima ejecución real.

## Evidencia estática ejecutada

Archivo:

```text
docs/validation/SEG-001-static-automation-2026-07-20.md
```

Resultados actuales:

```text
Compose YAML: PASS
servicios postgres/backend/frontend/smoke: presentes
smoke -> frontend healthy: presente
CI YAML: PASS
job compose-images-and-smoke: presente
paso Run containerized smoke test: presente
preflight.sh sh -n: PASS
smoke-test.sh sh -n: PASS
Makefile básico make -n: PASS
Makefile smoke-container make -n: PASS
```

Son controles de sintaxis/estructura, no ejecución funcional.

## Matriz

| Control | Estado | Evidencia |
|---|---|---|
| Consolidación en main | PASS | Fast-forward y comparación |
| Configuración DB | PASS_STATIC | `.env`/Compose/backend coherentes |
| Documentación | PASS_STATIC | README y guías |
| `.dockerignore` backend/frontend | PASS_STATIC | datos/secretos excluidos |
| `.gitattributes` | PASS_STATIC | eol multiplataforma |
| Compose YAML | PASS_SYNTAX | cuatro servicios |
| CI YAML | PASS_SYNTAX | cuatro jobs y smoke E2E |
| Scripts Unix | PASS_SYNTAX | `sh -n` |
| Makefile | PASS_SYNTAX | `make -n` incluido smoke-container |
| PowerShell syntax | IMPLEMENTED_NOT_RUN | pwsh ausente |
| Maven/Spotless | BLOCKED | Maven/red ausentes |
| Unit tests/ArchUnit | IMPLEMENTED_NOT_RUN | suite versionada |
| Testcontainers | BLOCKED | Docker ausente |
| Flyway/Hibernate | IMPLEMENTED_NOT_RUN | requiere PostgreSQL real |
| npm install | BLOCKED | registro/caché ausentes |
| package-lock | MISSING | generar/versionar |
| frontend typecheck/build | BLOCKED | dependencias ausentes |
| preflight real | NOT_RUN | requiere entorno completo |
| `docker compose config` | BLOCKED | Docker ausente |
| imagen backend | BLOCKED | Docker ausente |
| imagen frontend | BLOCKED | Docker/red ausentes |
| pull curl smoke image | BLOCKED | Docker/red ausentes |
| stack app | NOT_RUN | requiere Docker |
| smoke E2E container | NOT_RUN | requiere stack |
| smoke host Unix/Windows | NOT_RUN | requiere stack |
| GitHub Actions | NO_VISIBLE_RUN | sin statuses/runs |
| datos sensibles | PASS_STATIC | sin lote real |
| envío real ausente | PASS_BY_DESIGN | sin adaptador |

## CI implementado

### backend

- Maven verify;
- Spotless;
- unit tests;
- ArchUnit;
- Testcontainers.

### frontend

- npm install;
- typecheck;
- build.

### scripts

- sintaxis Unix;
- sintaxis PowerShell;
- preflight fail-closed.

### compose-images-and-smoke

- `.env` ficticio fail-closed;
- Compose perfiles app/smoke;
- build backend;
- build frontend;
- arranque PostgreSQL/backend/frontend;
- contenedor smoke;
- health + Basic Auth + frontend;
- logs en fallo;
- cleanup con volumen de CI.

Caché npm desactivada hasta lockfile.

## Correcciones estáticas acumuladas

1. exclusiones importadas retroactivas/auditadas;
2. ambigüedades persistidas en preview;
3. duplicados exactos enlazados;
4. límites 10 MB/HTTP 413;
5. orden hoja/fila;
6. validación y recuperación por fila;
7. CSV endurecido;
8. fechas UTC;
9. preview con exclusiones;
10. `excludedRows` separado;
11. filename seguro;
12. Basic Auth UTF-8;
13. DB local coherente;
14. main canónico;
15. stack app;
16. imágenes y health checks;
17. smoke host y container;
18. PowerShell corregido;
19. CI sin caché npm prematura;
20. build contexts seguros.

## Evidencia estructural

- main contiene todo;
- Flyway V1–V5;
- guardas de envío cerradas;
- fixtures `.test`;
- lote real ausente;
- Maven Wrapper verificado;
- perfiles app/smoke;
- puertos loopback;
- imágenes multi-stage;
- Nginx proxy;
- smoke solo lectura;
- scripts y Makefile;
- CI E2E preparado.

## Entorno disponible

```text
Java/Node/npm: presentes
sh/make/PyYAML: presentes
PowerShell: ausente
Maven: ausente
Docker/Compose: ausentes
cachés: vacías
registros externos: inaccesibles
workflow dispatch conector: no disponible
statuses/runs: no visibles
```

## Protocolo para PASS

1. clonar main con red/Docker;
2. registrar SHA;
3. configurar `.env` fail-closed;
4. preflight;
5. Maven/Spotless/tests;
6. Flyway/Hibernate/Testcontainers;
7. npm install + lockfile;
8. npm ci/typecheck/build;
9. Compose app/smoke;
10. imágenes/pulls;
11. smoke E2E;
12. escaneo local;
13. corregir con regresión;
14. repetir matriz;
15. registrar comandos/salidas;
16. cerrar SEG-001 solo con PASS.

## Condición de cierre

No se cumple. `PASS_BY_DESIGN` solo aplica a la ausencia deliberada de envío.
