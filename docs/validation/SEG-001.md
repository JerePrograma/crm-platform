# Validación SEG-001

## Estado

`PENDING_EXECUTION — STATIC_REVIEW_COMPLETE`

Este archivo distingue evidencia ejecutada, revisión estática y expectativas. No convertir una prueba versionada en una prueba aprobada.

## Rama

```text
feat/seg-001-prospect-vertical-slice
```

## Revisión actual

- fecha: `2026-07-20`;
- último commit funcional revisado: `48ab6ef49cd193449f285b950f75561106af250f`;
- comparación previa a documentación: 163 commits adelante de `main`, 0 atrás;
- PR: inexistente;
- merge/despliegue: inexistentes.

## Matriz de controles

| Control | Comando/job | Estado | Evidencia |
|---|---|---|---|
| Maven/compilación | `sh ./mvnw -B -f backend/pom.xml verify` | BLOCKED | Maven ausente, sin caché y sin resolución de red |
| Spotless | fase `verify` | BLOCKED | Depende de Maven |
| Unit tests | Surefire | IMPLEMENTED_NOT_RUN | Nuevas regresiones versionadas |
| Testcontainers | Surefire + Docker | BLOCKED | Docker ausente |
| Flyway V1–V5 | contextos Testcontainers | IMPLEMENTED_NOT_RUN | Migraciones revisadas estáticamente |
| Hibernate validate | contextos Testcontainers | IMPLEMENTED_NOT_RUN | Entidades cruzadas con esquema |
| Frontend TypeScript | `npm run build` | BLOCKED | Sin dependencias ni caché npm |
| Lockfile frontend | `npm install` | BLOCKED | Sin acceso al registro |
| Compose | `docker compose config` | BLOCKED | Docker/Compose ausentes |
| Imagen backend | `docker build ...` | BLOCKED | Docker ausente |
| GitHub Actions | workflow `ci` | NO_VISIBLE_RUN | Sin checks y sin herramienta de dispatch |
| Secret/data scan | búsquedas remotas | PASS_STATIC | Sin claves, tokens, correos personales ni XLSX real |
| Envío real ausente | inspección de árbol | PASS_BY_DESIGN | Sin Gmail/SMTP/mail adapter |

## Hallazgos de revisión estática corregidos

1. **Exclusiones importadas incompletas**: ahora atraviesan `ExclusionApplicationService`, deshabilitan prospectos existentes y generan auditoría.
2. **Preview ambiguo sin detalle**: ahora persiste `DuplicateReview` incluso en dry-run.
3. **Duplicado exacto sin referencia**: `ImportRow` conserva el prospecto existente.
4. **Límites contradictorios**: multipart y servicio comparten 10 MB; exceso HTTP devuelve 413/RFC 7807.
5. **Orden no determinístico**: filas ordenadas por hoja y número.
6. **Correo interno sin validación**: normalización rechaza formatos inválidos antes de persistencia.
7. **Doble fallo al registrar rechazo**: normalización tolerante para evidencia de filas fallidas.
8. **CSV débil**: soporte `,`/`;`, rechazo de comillas abiertas y encabezados duplicados.
9. **Fecha dependiente del host**: fechas Excel transformadas en UTC.
10. **Preview no evaluaba exclusiones**: aplica la misma elegibilidad sin escrituras de dominio.
11. **Bloqueados mezclados con aceptados**: migración V5 y `excludedRows` separado.
12. **Nombre de archivo no saneado**: basename sin rutas ni caracteres de control.
13. **Basic Auth no Unicode-safe**: codificación UTF-8 en frontend.

## Pruebas de regresión añadidas

- exclusión importada suprime prospecto existente y crea auditoría;
- preview persiste revisión ambigua sin crear otro prospecto;
- duplicado exacto enlaza `ImportRow.prospect`;
- correo malformado rechaza solo su fila y completa el trabajo;
- preview clasifica como `EXCLUDED` sin escribir dominio;
- conteo `excludedRows` independiente;
- CSV separado por `;` con delimitador dentro de comillas;
- CSV con comillas sin cerrar;
- encabezados duplicados después de normalización;
- correo inválido rechazado por normalización.

## Evidencia estructural

- rama real y aislada de `main`;
- Flyway V1–V5 aditivo;
- configuración de envío cerrada;
- archivos remotos leídos tras escritura;
- fixture únicamente con dominios `.test`;
- XLSX real ausente del árbol;
- workflow de backend/frontend/Compose/imagen presente;
- launcher Maven con hash fijado;
- sin acción de envío real.

## Bloqueo externo reproducido

Entorno disponible:

```text
Java: presente
Node/npm: presentes
Maven: ausente
Docker/Compose: ausentes
~/.m2: vacío
npm cache: vacío
DNS/acceso GitHub y registros: no disponible
GitHub connector workflow_dispatch: no disponible
Checks asociados al commit: ninguno visible
```

## Protocolo para convertir a PASS

1. ejecutar CI o clonar en un entorno con red;
2. registrar commit exacto;
3. correr Maven/Spotless/tests/Testcontainers;
4. generar lockfile y construir frontend;
5. validar Compose e imagen;
6. corregir todos los fallos con regresión;
7. ejecutar escaneo local de secretos;
8. completar la tabla con fecha, run/job y salida;
9. marcar `SEG-001` completo solo si todos los controles principales están en `PASS`.

## Condición de cierre

No se cumple todavía. `PASS_BY_DESIGN` solo se acepta para la ausencia deliberada de envío real.
