# SEG-001 — Vertical slice persistente de prospectos

## Estado

```text
COMPLETE
IMPLEMENTED
HARDENED
CONSOLIDATED_IN_MAIN
COMPLETE_VALIDATION_AUTOMATED_WINDOWS_AND_UNIX
POWERSHELL_SYNTAX_PASS
PREFLIGHT_PASS
WINDOWS_AND_DOCKER_PORT_CHECK_PASS
POSTGRES_PUBLICATION_PASS
POSTGRES_HEALTH_PASS
FRONTEND_CLEAN_BUILD_PASS
BACKEND_CLEAN_IMAGE_BUILD_PASS
FLYWAY_V1_V5_PASS
HIBERNATE_VALIDATE_PASS
THREE_SERVICES_HEALTHY
SMOKE_HOST_AND_CONTAINER_PASS
MAVEN_VERIFY_PASS
TESTS_SPOTLESS_ARCHUNIT_TESTCONTAINERS_PASS
LOCKFILE_VERSIONED
SECOND_CLEAN_NPM_CI_RUN_PASS
REPOSITORY_SAFETY_PASS
CI_VISIBLE_GREEN
```

## Objetivo

Entregar una vertical slice persistente y segura para:

- instituciones;
- contactos y canales;
- prospectos;
- exclusiones dominantes;
- importación CSV/XLSX con preview y ejecución confirmada;
- deduplicación exacta y revisión ambigua;
- auditoría;
- API autenticada;
- interfaz web operativa;
- stack Docker reproducible.

## Alcance implementado

### Backend

- Java 21 y Spring Boot 4.1;
- PostgreSQL como fuente de verdad;
- Flyway y migraciones versionadas;
- Hibernate `ddl-auto=validate`;
- REST/OpenAPI/RFC 7807;
- Basic Auth bootstrap fail-closed;
- métricas y health;
- importación idempotente y auditada;
- Testcontainers, unit tests y ArchUnit.

### Frontend

- React, TypeScript strict y Vite;
- autenticación bootstrap;
- Dashboard;
- exclusiones;
- preview e importación confirmada;
- prospectos;
- auditoría;
- Nginx y proxy al backend.

### Operación

- Docker Compose con perfiles `app` y `smoke`;
- puertos host configurables y loopback-only;
- preflight Windows/Unix;
- checker de sintaxis PowerShell;
- checker de propiedad Docker/enlace Windows;
- clean builds;
- smoke host y contenedor;
- Maven verify/Testcontainers en Docker;
- generación segura del lockfile;
- validadores integrales PowerShell/Bash;
- JSON/transcript fuera de Git;
- escaneo de seguridad.

## Evidencia real de cierre

Aprobado:

```text
PowerShell syntax
preflight container-only
Docker daemon y Compose config
publicaciones Docker y bind Windows
PostgreSQL publication en 25432
PostgreSQL health
frontend clean build --no-cache
TypeScript strict
Vite production build
backend clean image build --no-cache
Maven package -DskipTests (PASS_PARTIAL de imagen)
Flyway V1–V5 antes de Hibernate
PostgreSQL, backend y frontend healthy
smoke host y contenedor
Maven verify
29/29 tests
Spotless 55/55
ArchUnit
Testcontainers
lockfile versionado
segunda corrida limpia mediante npm ci
repository safety
GitHub Actions verde
```

La ejecución de origen sobre `a9e2c44…` ya había superado Flyway e Hibernate,
pero falló al crear `AuditEventWriter` porque el código propio pedía el mapper
Jackson 2 y Spring Boot 4 administraba Jackson 3. El código fue alineado con
`tools.jackson`, se agregó regresión de contexto/persistencia JSONB y se corrigieron
los siguientes fallos reales de JDBC, Testcontainers y healthcheck.

Segunda validación integral limpia:

```text
commit: d8a5a449a72c660e2655f4be7144360cd1e719a4
resultado: PASS
lockfile SHA-256: 1936217c0598825ef43519069a3ba89a974e2b30e3b9f2619d4e62dd10810c98
CI run 29848718163: success
```

Evidencia:

```text
docs/validation/SEG-001-jackson-objectmapper-failure-2026-07-21.md
validation-output/seg001-complete-20260721-133002.log
validation-output/seg001-complete-20260721-133002.json
validation-output/seg001-docker-20260721-133003.json
```

`validation-output/` permanece fuera de Git.

## Correcciones versionadas y ejecutadas

- `spring-boot-starter-flyway` reemplaza la dependencia directa `flyway-core`;
- `flyway-database-postgresql` se conserva;
- `spring.flyway.fail-on-missing-locations=true` activa fail-fast de recursos.

Commits:

```text
50a6b1b7c6eedd45da9a8af1462b76e00ff64427
a3782c42fdf6c84e83ad8adcebd8770d41438098
f39e881 fix: align audit JSON serialization with Spring Boot 4
6f0bafb style: apply backend formatter
41a1548 fix: support current Docker API in backend verification
951d19b fix: use IPv4 frontend health check
d8a5a44 build: lock frontend dependencies
```

## Criterios de aceptación

- [x] Flyway auto-configurado en ejecución real;
- [x] `flyway_schema_history` creada o validada;
- [x] migraciones V1–V5 aplicadas;
- [x] Hibernate validate PASS;
- [x] PostgreSQL, backend y frontend healthy;
- [x] smoke host PASS;
- [x] smoke contenedor PASS;
- [x] Maven verify PASS;
- [x] Spotless PASS;
- [x] unit tests PASS;
- [x] ArchUnit PASS;
- [x] Testcontainers PASS;
- [x] `package-lock.json` generado y revisado;
- [x] lockfile versionado;
- [x] ejecución limpia mediante `npm ci`;
- [x] seguridad final PASS;
- [x] evidencia JSON/transcript final;
- [x] CI verde visible;
- [x] documentación final sincronizada.

## Fuera de alcance

- campañas;
- Gmail/SMTP;
- Sheets/Drive;
- workers de envío;
- despliegue cloud o producción;
- importación del lote operativo real;
- resolución UI de revisiones ambiguas;
- jobs con retry.

## Seguridad obligatoria

```text
SENDING_ENABLED=false
SENDING_DRY_RUN=true
SENDING_DAILY_LIMIT=0
SENDING_KILL_SWITCH=true
```

No versionar `.env`, `validation-output/`, datos privados, claves o credenciales. No usar `docker compose down -v` salvo destrucción intencional. No activar SEG-002 hasta completar esta matriz.
