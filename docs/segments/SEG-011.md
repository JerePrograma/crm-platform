# SEG-011 — Validación integral y cierre

Actualizado: 2026-07-22

Estado: `IN_PROGRESS` hasta completar dos corridas limpias consecutivas de
`scripts/validate-complete-crm.ps1` sobre el mismo commit candidato.

## Contrato

SEG-011 demuestra, no presume:

- rama esperada y árbol limpio;
- repository safety y secret scan;
- backend 79/79, Spotless, ArchUnit y PostgreSQL/Testcontainers;
- frontend npm ci, TypeScript, Vitest, build y Playwright;
- imágenes sin caché, health y smoke autenticado;
- Flyway vacío→V13 y V11→V12→V13, Hibernate validate;
- outbox/workers/retry/dead-letter, inbound/HMAC/replay/quarantine;
- RBAC, tenant isolation, reportes, búsqueda, settings y CSV seguro;
- npm audit y Grype High/Critical;
- cuatro bloqueos, endpoint send ausente y cero estados enviados;
- backup/restore sintético y perfil productivo local;
- evidence JSON/transcript fuera de Git;
- repetición limpia sin flakiness.

## Estados externos permitidos

- Gmail/WhatsApp reales: `IMPLEMENTED_NOT_CONNECTED` /
  `BLOCKED_EXTERNAL_CREDENTIALS`;
- XLSX real: `BLOCKED_EXTERNAL_FILE` si no está disponible;
- producción: `NOT_AUTHORIZED` y `NOT_DEPLOYED`;
- validación Unix funcional: `NOT_RUN` o `BLOCKED_PLATFORM` si no hay host;
- CI remoto: `NOT_RUN` hasta push autorizado.

Ningún estado externo puede convertirse en `EXECUTED_PASS` por inspección o por
tener código escrito.
