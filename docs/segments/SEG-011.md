# SEG-011 — Validación integral y cierre

Actualizado: 2026-07-22

Estado: `COMPLETE`.

Las dos corridas integrales Windows se ejecutaron sobre el commit funcional
`986523a22a17f4c9159003bce88f4f6903ad7cdb`. La integración posterior en
`main` y este cierre documental no modifican el código probado.

## Contrato

SEG-011 demuestra, no presume:

- rama esperada y árbol limpio;
- repository safety y secret scan;
- backend 79/79, Spotless, ArchUnit y PostgreSQL/Testcontainers;
- frontend npm ci, TypeScript, Vitest, build y Playwright;
- imágenes sin caché, health y smoke autenticado;
- Flyway vacío→V13 y V11→V12→V13, Hibernate validate;
- outbox/workers/retry/dead-letter, inbound/HMAC/replay/quarantine;
- RBAC, tenant isolation, reportes, búsqueda, settings y CSV seguro;
- npm audit y Grype High/Critical;
- cuatro bloqueos, endpoint send ausente y cero estados enviados;
- backup/restore sintético y perfil productivo local;
- evidence JSON/transcript fuera de Git;
- repetición limpia sin flakiness.

## Estados externos permitidos

- Gmail/WhatsApp reales: `IMPLEMENTED_NOT_CONNECTED` /
  `BLOCKED_EXTERNAL_CREDENTIALS`;
- XLSX real: `BLOCKED_EXTERNAL_FILE` si no está disponible;
- producción: `NOT_AUTHORIZED` y `NOT_DEPLOYED`;
- validación Unix integral: `IMPLEMENTED_NOT_RUN`;
- CI remoto antes de la publicación actual: `IMPLEMENTED_NOT_RUN`.

Ningún estado externo puede convertirse en `EXECUTED_PASS` por inspección o por
tener código escrito.

## Evidencia ejecutada

Comando canónico:

```powershell
powershell -ExecutionPolicy Bypass -File scripts/validate-complete-crm.ps1 `
  -PostgresPort 25432 `
  -BackendPort 8080 `
  -FrontendPort 5173
```

Resultados sobre `986523a22a17f4c9159003bce88f4f6903ad7cdb`:

```text
RUN_1=FUNCTIONAL_PASS; DURATION=713,870 s; PHASES=21/21
RUN_2=FUNCTIONAL_PASS; DURATION=734,162 s; PHASES=21/21
REPRODUCIBILITY=PASS
FLAKINESS=NOT_OBSERVED
```

Ambas corridas validaron 79/79 backend, Spotless 159/159, ArchUnit, Vitest 2/2,
Playwright 2/2, imágenes sin caché, health, smoke autenticado, migraciones
vacío→V13 y V11→V13, Hibernate validate, outbox, workers, inbound, cuatro
bloqueos de envío, cero `SENT|DELIVERED|READ`, backup/restore, perfil productivo
validado localmente, npm audit, Grype y repository safety.

Evidencia sanitizada:

`docs/validation/SEG-011-complete-crm-closure-2026-07-22.md`

Frontera conservada:

```text
UNIX_INTEGRAL=IMPLEMENTED_NOT_RUN
GMAIL_REAL=IMPLEMENTED_NOT_CONNECTED
WHATSAPP_REAL=IMPLEMENTED_NOT_CONNECTED
REAL_COMMUNICATIONS=DISABLED_BY_POLICY
PRODUCTION=NOT_AUTHORIZED
PRODUCTION_DEPLOYMENT=NOT_DEPLOYED
```
